4	Resolving Session Fixation in JBoss	.../deploy/jboss-web.deployer/server.xml
13	How to retrieve a file from a server via SFTP?	JSch jsch = new JSch();\n\nString knownHostsFilename = "/home/username/.ssh/known_hosts";\njsch.setKnownHosts( knownHostsFilename );\n\nSession session = jsch.getSession( "remote-username", "remote-host" );    \n{\n  // "interactive" version\n  // can selectively update specified known_hosts file \n  // need to implement UserInfo interface\n  // MyUserInfo is a swing implementation provided in \n  //  examples/Sftp.java in the JSch dist\n  UserInfo ui = new MyUserInfo();\n  session.setUserInfo(ui);\n\n  // OR non-interactive version. Relies in host key being in known-hosts file\n  session.setPassword( "remote-password" );\n}\n\nsession.connect();\n\nChannel channel = session.openChannel( "sftp" );\nchannel.connect();\n\nChannelSftp sftpChannel = (ChannelSftp) channel;\n\nsftpChannel.get("remote-file", "local-file" );\n// OR\nInputStream in = sftpChannel.get( "remote-file" );\n  // process inputstream as needed\n\nsftpChannel.exit();\nsession.disconnect();
34	Is there a reason to use BufferedReader over InputStreamReader when reading all characters?	InputStream stream = new FileInputStream("in");\n    try { //no operations between open stream and try block\n        //work\n    } finally { //do nothing but close this one stream in the finally\n        stream.close();\n    }
45	Struct like objects in Java	public property String foo;   \na-&gt;Foo = b-&gt;Foo;
51	How to find a normal vector pointing directly from virtual world to screen in Java3D?	Vector3f toScreenVector = new Vector3f(0, 0, 1);\n\nTransform3D t3d = new Transform3D();\ncanvas.getImagePlateToVworld(t3d);\nt3d.transform(toScreenVector);\n\ntg.getTransform(t3d); //tg is Transform Group of all objects in a scene\nt3d.transform(toScreenVector);
64	Find all drive letters in Java	File[] roots = File.listRoots();\nfor(int i = 0; i &lt; roots.length ; i++)\n    System.out.println("Root["+i+"]:" + roots[i]);
72	Java, UTF-8, and Windows console	chcp 65001 &amp;&amp; start.bat
73	Find the settings JNDI is using for error reporting	InitialContext.getEnvironment().get(Context.PROVIDER_URL)
75	Stuts2 Tiles Tomcat suspected of changing UTF-8 to?	&lt;%@ page contentType="text/html; charset=UTF-8" %&gt;
76	Iterators in C++ (stl) vs Java, is there a conceptual difference?	transform
82	Can I convert the following code to use generics?	protected static &lt;T extends Comparable&lt;T&gt;&gt; int nullCompare(T o1, T o2) {
86	How to escape text for regular expression in Java	Pattern.quote("$5");
99	Problem with unicode String literal in unit test	javac -encoding UTF-8
101	Reading from a ZipInputStream into a ByteArrayOutputStream	zipStream.read(tempBuffer)
106	Can I add maven repositories in the command line?	mvn package -Dmaven.repo.remote=http://www.ibiblio.org/maven/,http://myrepo \n  -Dmaven.repo.local="c:\test\repo"
115	Using Spry inside Java Server Faces framework	&lt;div id="Accordion1" class="Accordion"&gt;\n            &lt;div class="AccordionPanel"&gt;\n                &lt;div class="AccordionPanelTab"&gt;Panel 1&lt;/div&gt;\n                &lt;div class="AccordionPanelContent"&gt;\n                    Panel 1 Content&lt;br/&gt;\n                    Panel 1 Content&lt;br/&gt;\n                    Panel 1 Content&lt;br/&gt;\n                &lt;/div&gt;\n            &lt;/div&gt;\n    &lt;/div&gt;
145	How can I catch AWT thread exceptions in Java?	class AWTExceptionHandler {\n\n  public void handle(Throwable t) {\n    try {\n      // insert your exception handling code here\n      // or do nothing to make it go away\n    } catch (Throwable t) {\n      // don't let the exception get thrown out, will cause infinite looping!\n    }\n  }\n\n  public static void registerExceptionHandler() {\n    System.setProperty('sun.awt.exception.handler', AWTExceptionHandler.class.getName())\n  }\n}
170	Access Enum value using EL with JSTL	&lt;c:when test="${someModel.status == 'OLD'}"&gt;
194	Get the time of tomorrow xx:xx	// make it now\nCalendar dateCal = Calendar.getInstance();\n// make it tomorrow\ndateCal.add(Calendar.DAY_OF_YEAR, 1);\n// Now set it to the time you want\ndateCal.set(Calendar.HOUR_OF_DAY, hours);\ndateCal.set(Calendar.MINUTE, minutes);\ndateCal.set(Calendar.SECOND, seconds);\ndateCal.set(Calendar.MILLISECOND, 0);\nreturn dateCal.getTime();
199	Java: Enum parameter in method	private enum Alignment { LEFT, RIGHT };    \nString drawCellValue (int maxCellLength, String cellValue, Alignment align){\n  if (align == Alignment.LEFT)\n  {\n    //Process it...\n  }\n}
